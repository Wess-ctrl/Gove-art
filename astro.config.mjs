// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://Wess-ctrl.github.io', // Cambia 'tu-usuario' por tu usuario real de GitHub
  base: '/Gove-art',    // Cambia por el nombre exacto de tu repositorio en GitHub
});
